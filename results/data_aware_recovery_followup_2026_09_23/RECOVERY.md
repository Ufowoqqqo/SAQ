# 可恢复性补查：有路径，但未找到原件

2026-09-23，主机 rwcpu8.cse.ust.hk；固定源码 ca05b90。

**恢复原件仍被阻塞；若能找回原训练好的 PCA 和 coarse 状态，恢复同一数学表征不需要百万行中间缓存。** 这是对上一份阻塞报告中恢复依赖的进一步缩小，不是已恢复或已完成实验。

| 路径 | 最小所需对象 | 当前状态／边界 |
|---|---|---|
| 找回原面板 | SIFT、GIST 各一个 residual_nlist1024.fvecs，及来源／哈希凭据 | 每个 8,454,144 bytes；冻结索引已恢复，但未找到面板 |
| 使用原变换重算 | 每个数据集的 pca.faiss 与 nlist_1024/coarse.faiss，加原始 base 和冻结索引 | 不需持久化百万行 base_pca 或 assignments；四个学习产物仍缺失 |
| 重新训练 | 官方 learn、base、冻结训练源码及依赖 | 技术上可重建流程，但不等于找回原产物；本轮用户明确禁止重训，未执行 |

原变换重算的步骤是：读取冻结行，执行完整维度 PCA，使用全维 coarse centroids 做最近中心路由，减中心后取前 128 维。GIST 必须在 960 维路由，不能先截至 128 维。源码定义在 `research/structured_2d/common.cpp:259,300` 和 `research/correlated_pair_allocation/extract_stage_panels.cpp:167`。

当前 extractor 硬编码读取 1024/4096 两套状态及百万行中间文件，不能直接运行上述最小恢复；如果找回原变换，需要一个只恢复 1024 选定行的聚焦改动。尚未编写或执行此改动。

**不能承诺旧面板字节一致。** 原批次为 SIFT 16,384 行、GIST 4,096 行；改变批次或 BLAS 路径可能改变浮点舍入，临近距离并列时也可能影响路由。9/3 报告已记录单行 PCA 重放与旧缓存的微小差异。需原面板／哈希、原选定 PCA 行与标签，或其他足够的原产物对照，才能证明恢复一致性。不能把“同源码同种子”直接当作旧输入身份核验。

## 新查到的线索

- `/usr/lib/tmpfiles.d/tmp.conf` 实际配置为 `q /tmp 1777 root root 10d`；9/3 缓存有可能已被自动清理，但这只是解释线索，未通过删除日志确认原因。
- 本机可见的 `.snapshot`、`.snapshots` 和用户 Trash 入口均未发现。未探测未知远程主机，未联系管理员；不能由此排除服务器备份或其他机器上的副本。
- 本地 Git 全部可达 refs 的对象路径清点未见目标面板、PCA 或 coarse 文件；历史文档也明确这些产物只放在 `/tmp`、未提交。Git 能恢复代码，当前没有发现它保存这批输入的证据。
- 追加尝试核验 GIST base 全文件 SHA-256，达到 120 秒墙钟限额后以 124 退出，未得到完整摘要，不重试。GIST 仍只有本次大小核验；不能写成哈希已通过。命令与退出状态见资源日志。

本次进一步定位窗口 21:51:36–21:58:06 HKT，6 分 30 秒；与首轮 8 分 34 秒合计 15 分 04 秒，未用满 30 分钟恢复预算。没有进行数据下载、解包、IVF 重训、量化实验或查询数据读取。记录时间后的工作仅整理结果。

## 若未来另行选择重建

缺少两份官方 learn：SIFT 为 51,600,000 bytes，GIST 为 1,922,000,000 bytes。不能用 base 替代。
依赖为固定 Faiss `0ca9df4792b173d573044ee14ca0704780176e82`，coarse seed 1234、25 次迭代、1 redo、非 spherical；还需核实编译器、BLAS 和线程设置。

历史完整 common 重建使用 5,921 CPU 秒（约 1.64 CPU 小时），包括两数据集、两种 nlist 和百万行转换。它只能作为成本参考，不能保证当前 4 线程及剩余预算内完成；只建 1024 并处理选定行可能更省，尚未测量。

建议顺序：优先从原运行主机或已有备份寻找两个面板；其次寻找四个原学习状态文件。若两者均无备份，另行明确是否允许重建、取得缺失 learn，并将产物标记为重建面板，而非声称原面板已恢复。重建还需评估是否满足原实验的历史输入要求。

当前决定仍为 BLOCKED。没有请求或执行重训，没有修改科学源码，没有 push 或发送邮件；配对效应与位分配效应仍未测量。前一轮输出及其校验清单保持不变。
