# A4-V2 SOURCE-HISTORY-I-R1-P path-closure erratum protocol

Date: 2026-07-18

Stage: `A4-V2-SOURCE-HISTORY-I-R1-P`

Authority: the user's exact instruction

```text
授权：
A4-V2-SOURCE-HISTORY-I-R1-P
correction-only path-closure erratum + direct-child review
```

## 1. Smallest question, scope, and ceiling

The smallest falsifiable question is whether one additive documentation-only
erratum can correct the impossible ten-path implementation projection in the
reviewed source-history protocol, while preserving every scientific,
source-semantic, epoch, identity, overhead, and execution boundary already
frozen by that protocol.

This stage forms only this erratum, its machine contract, focused root status,
one immutable target, and one exact direct-child independent review.  It does
not edit the original protocol objects, verifier source, derived authority,
schema, witness, manifest, binding, or crosswalk.  With zero findings at LOW
severity or above, its maximum outcome is:

```text
SOURCE_HISTORY_I_R1_PATH_CLOSURE_ERRATUM_REVIEW_PASS
```

That outcome does not implement the epoch correction, establish cache-verifier
or PAR readiness, authorize PREP/CACHE-BIND/PAR, or provide feasibility,
performance, SAQ-limitation, novelty, systems, or method evidence.

## 2. Immutable reviewed authority and pre-target blocker

The immutable source-history protocol target and its publication-closed review
are preserved exactly:

| Object | Commit | SHA-256 | Bytes | Git blob |
| --- | --- | --- | ---: | --- |
| protocol | `dcaed57aaae6fe0f120377281921b6ea5336eb7f` | `289e0acb8c1e8122e027fad1757f9fcc6b721ccf49affebce1141f51adfe2650` | 16,130 | `2297c34e227a1ad7fe21d15b2674a4987e44e858` |
| machine contract | `dcaed57aaae6fe0f120377281921b6ea5336eb7f` | `d8150681aa9d7116d1d131ca61f9a027ea737b3a6245bf8ee2e000b88548ea48` | 11,379 | `9c08aaf63d1e524cfa3579a764fd8ea14176cde8` |
| protocol review | `e98a3e401639cae4f11ea97d21883120e9b797fb` | `58bf7a3deb89e1ba9ab0bbe63c6f66e96d1abf6c44d2c7e47189bd38388c627e` | 13,478 | `d59a56082c52eec113856eb33e918999415f6201` |

The authorized `A4-V2-SOURCE-HISTORY-I` pre-target audit found one closure
BLOCKER and stopped without a target.  Protocol section 5 and
`future_implementation.exact_target_changed_paths` require all ten registered
paths to change.  The legitimate deterministic identity cascade changes only
eight.  The two unsupported required paths are:

```text
docs/saq_a4_v2_cache_runtime_maximal_instance_2026_07_15.json
docs/saq_a4_v2_cache_runtime_schema_2026_07_15.json
```

A provisional verifier edit was fully reverted.  No WIP, source commit,
implementation review, execution, or scientific evidence resulted.

## 3. Why the two paths must remain byte-identical

The frozen source correction changes only the verifier's internal expected-map
selection for ten already registered history roles and admits one ninth
protocol-component identity through the existing loop.  It adds no output
field, status, mismatch shape, parser, Git observation, history role, retry,
timer, cap, or ledger field.

The runtime schema already represents source checks generically, fixes their
cardinality at 37, permits the unchanged `PASS`/`MISMATCH` statuses, and types
`filesystem_bytes_read` as the existing `uint64`.  The additional component
read therefore changes a future observed value but not its schema.  The
runtime maximal instance already uses 37 generic maximal source checks, 256
generic maximal mismatches, and the maximum `uint64` ledger value.  It binds
no active source identity, protocol-component set, or role-to-epoch map.

Their immutable identities at the erratum parent are:

| Preserved object | SHA-256 | Bytes | Git blob |
| --- | --- | ---: | --- |
| runtime schema | `47bfcd039acddebbf9c6e5058b1c20ca9a743a5d2518ff31d4fef72ae103f896` | 23,762 | `4d0c93f7fea77fec649ff2a9ab4168f1a340a3cf` |
| runtime maximal instance | `5a373bd002b48041e150588002a193a9c068cf98977897e6f5cc5f3339e71a42` | 4,439,071 | `51a03242b7ef667a4bd4275d8565122f49222674` |

Changing a comment, claim string, maximal filler, or adding an epoch/output
field merely to force a Git delta would not be an identity cascade.  It would
violate the original protocol's closed semantic delta and explicit ban on new
output and ledger fields.  These two blobs must therefore remain exact.

## 4. Additive correction-only supersession

This erratum supersedes only the original protocol's ten-path future target
projection, the statement that seven derived objects change, and the matching
review assertion of ten-path necessity.  For a separately authorized R1
implementation, those become an eight-path target and five changed derived
objects.  No original byte is rewritten and the failed pre-target attempt
remains a truthful stopped checkpoint.

All other clauses of the original protocol and contract remain authoritative,
including:

- exactly five legacy Git-blob overrides;
- exactly four `LEGACY_PRE_HOST_REBIND` roles and six
  `ACTIVE_CORRECTED_SOURCE` roles, assigned before observation;
- rejection of either-epoch and every adaptive selection rule;
- unchanged current-source closure, `kind=blob`, statuses, parser,
  observations, caps, ledgers, precedence, retries, and null-role semantics;
- exactly one changed source and at most 80 net changed verifier lines;
- the approximate 25--45-line semantic-core expectation;
- exactly one ninth protocol component, with no tenth component; and
- every execution, readiness, performance, and scientific prohibition.

The ninth component remains the original contract, not this erratum:

```text
source_history_epoch_correction_contract
docs/saq_a4_v2_source_history_epoch_correction_contract_2026_07_17.json
```

This erratum and its contract are governance authority only.  They must not be
added to `PROTOCOL_COMPONENT_KEYS`, `PROTOCOL_COMPONENT_PATHS`, or the active
cache-authority protocol-component map.

## 5. Exact future R1 target and review topology

Only after this erratum target and its direct-child review are committed,
pushed, handed off, and pass with zero LOW-or-higher findings may the user
separately authorize exactly:

```text
A4-V2-SOURCE-HISTORY-I-R1
```

The future target must be the direct child of the clean, pushed,
publication-closed erratum-review head.  It changes all and only these eight
mode-`100644` paths:

```text
AGENTS.md
TASK.md
docs/saq_a4_v2_cache_protocol_authority_manifest_2026_07_15.json
docs/saq_a4_v2_cache_static_closure_2026_07_15.json
docs/saq_a4_v2_implementation_binding_2026_07_14.md
docs/saq_a4_v2_implementation_manifest_2026_07_14.json
docs/saq_a4_v2_source_provenance_crosswalk_2026_07_14.md
script/a4_v2_cache_policy_verifier.py
```

The five derived objects are the complete cascade: static closure records the
changed verifier identity and source tree; cache authority records that source
closure, the changed static-closure identity, and the original protocol
contract as component nine; binding and crosswalk record the immutable
protocol/review authority and resulting source/derived identities; and the
implementation manifest records the changed source/tree and dependent whole-
document identities.  The manifest schema, existing `authorization_identity`,
and `build_status=NOT_AUTHORIZED_NOT_RUN` remain unchanged.

The future direct-child review projection remains the original exact three
mode-`100644` paths:

```text
AGENTS.md
TASK.md
docs/saq_a4_v2_source_history_epoch_correction_implementation_independent_review_2026_07_17.md
```

Before review the maximum status is
`SOURCE_HISTORY_EPOCH_CORRECTION_R1_TARGET_FORMED_REVIEW_PENDING`.  With zero
LOW-or-higher findings, the maximum result remains only
`SOURCE_HISTORY_EPOCH_CORRECTION_SOURCE_STATIC_REVIEW_PASS`.

## 6. Current erratum target and direct-child review

This erratum target is the direct child of clean, pushed protocol-review head
`e98a3e401639cae4f11ea97d21883120e9b797fb`, tree
`dfc5df62443618c4af18391d25d28d5321c096de`.  It changes all and only:

```text
AGENTS.md
TASK.md
docs/saq_a4_v2_source_history_i_r1_path_closure_erratum_contract_2026_07_18.json
docs/saq_a4_v2_source_history_i_r1_path_closure_erratum_protocol_2026_07_18.md
```

Its exact direct-child independent review changes all and only:

```text
AGENTS.md
TASK.md
docs/saq_a4_v2_source_history_i_r1_path_closure_erratum_independent_review_2026_07_18.md
```

The reviewer may record PASS or FAIL but may not edit either erratum object.
Any finding at LOW severity or above forces FAIL and a user checkpoint.

## 7. Independent review requirements and overhead

The sole direct-child reviewer must independently establish:

1. exact parent, target, tree, push, mode, and four-path projection;
2. byte identity of the original protocol, contract, and review;
3. the original ten-path requirement and the pre-target stop without evidence;
4. absence of a legitimate runtime-schema or maximal-instance delta;
5. necessity and sufficiency of the corrected eight-path/five-derived-object
   cascade;
6. preservation of the epoch model, five overrides, ninth component, line
   limits, runtime semantics, overhead ledger, and future review projection;
7. explicit rejection of filler changes and any tenth component; and
8. every claim ceiling, prohibition, and excluded downstream question.

Scientific algorithmic core added is zero lines.  This protocol, its contract,
status, and review are permanent governance overhead.  No implementation or
support-code ratio, performance delta, hot path, or SOTA result follows;
`PERFORMANCE_NOT_YET_MEASURED` remains exact.

## 8. Prohibitions and excluded questions

Neither this erratum nor its review permits source or derived-authority edits,
history rewriting, schema/witness padding, Python, DNF, syntax/import,
compiler/build/test, repository execution, cache verifier, PREP, probe, clone,
START/token/receipt, CACHE-BIND, PAR-R1, SRUN, quarantine access, data/query/
index/result access, or host/environment mutation.

Fresh PREP lineage, CACHE-BIND schema/DAG closure, PAR-R1 authorization,
whole-host identity, and unknown static/runtime blockers remain separate.  An
eventual source-static PASS would remove only this known source-history rule;
it would not establish end-to-end readiness or scientific evidence.

## 9. Stop rule

R1-P stops after exact target/review commit, push, mandatory Meeting Summary
Handoff, and user report, or earlier on any LOW+ finding, dirty or unpublished
parent, unexpected path/mode, identity inconsistency, need to alter another
original rule, or need for execution.  Even after PASS, do not form the
eight-path source target without explicit `A4-V2-SOURCE-HISTORY-I-R1`
authorization.
