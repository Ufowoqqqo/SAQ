# A4-OR-C Synthetic Admission Authorization

Date: 2026-07-22

Branch: `saq-a4-original-reopening-protocol`

Reviewed parent: `7249dd6ccaa76b65997b5620f196d5bf4c2ff552`

User instruction: `授权 A4-OR-C`

Status: **AUTHORIZED / NOT YET IMPLEMENTED OR RUN**

## Authorized scope

The authorization opens only the `A4-OR-C` synthetic numerical/cost admission
defined by
`docs/saq_attempt4_original_reopening_protocol_2026_07_22.md`.

It permits:

- freezing the exact machine, dependency, synthetic-input, numeric, build,
  output, timing, and failure contract;
- pinning an official standard PQ/native k-means dependency with its revision
  and license;
- adding a new isolated `research/a4_or_c/` scientific implementation;
- compiling and running only the frozen tiny exact fixtures and deterministic
  same-shape synthetic admission;
- recording CPU/wall time, memory, numeric parity, representation parity,
  support/scientific ratios, and the terminal `A4-OR-C` status; and
- one bounded correctness/instrumentation repair if the protocol permits it,
  followed by one independent rereview.

It does not permit:

- reading GIST, CIFAR, centroids, cluster ids, benchmark queries, ground truth,
  generated indexes, old ignored results, or prior A4 outcome artifacts;
- executing or importing old A4-1S/V2 runners, CLIs, verifiers, archives, or
  evidence frameworks;
- modifying SAQ/CAQ, its index, packing, estimator, or search path;
- changing the original full-word mixed-radix mechanism into a prefix,
  progressive, pruning, or query-adaptive method;
- entering `A4-OR-B`, native query evaluation, or benchmark-query evaluation;
  or
- treating a synthetic pass as natural-data value, performance evidence,
  novelty, or a method contribution.

## Frozen question and stop condition

The only decision question is whether a production-like histogram/binary64
construction and the forced `D/A/P/V` shapes pass the registered tiny numeric,
packing/lookup, memory, support-overhead, and one-CPU-hour projected synthetic
admission.

Stop immediately with the registered failure status if any numeric fixture,
mixed-radix replay, control, shape, memory, or cost threshold fails. A pass
stops at `PASS_A4_OR_C_SYNTHETIC_ONLY`; it grants no later data authority.

## Expected implementation surface

Expected scientific-core files are confined to `research/a4_or_c/` and total
350--800 net lines, excluding the pinned third-party dependency. Expected
support/output code is at most the larger of 800 lines or twice the scientific
core. The old A4 exact/evidence sources remain unchanged and unlinked.

If these ceilings are exceeded before new decision-relevant evidence exists,
stop and return to the user rather than growing a custom evidence framework.
