# Root coordinator: research framing, routing, synthesis, and final decisions.
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"

[features]
multi_agent = true

[agents]
# Root + one bounded owner + one independent reviewer.
max_threads = 3
# Root may spawn direct children; children may not recursively delegate.
max_depth = 1
